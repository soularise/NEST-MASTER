@model IEnumerable<NEST.Models.ContentBody>

@{
    int i = 1;
    foreach (var item in Model) 

{
    if(i <= 3){
           
   
    <h2>@Html.DisplayFor(modelItem => item.Title)</h2>
    <p>
    @{if(item.BodyText.Length < 250){
        @Html.Raw(NEST.Common.HtmlRemoval.StripTagsRegex(item.BodyText.ToString()))
     }else
     {
        @Html.Raw(NEST.Common.HtmlRemoval.StripTagsRegex(item.BodyText.ToString()).Substring(0, 250))
     }
     }
     ... @Html.ActionLink("more", "../news/NewsDetails", new { id=item.Id })</p>       
                                                                                               
     <p align="right"><iframe src="//www.facebook.com/plugins/like.php?href=@Url.Content("~/news/NewsDetails/")@item.Id&amp;send=false&amp;layout=button_count&amp;width=50&amp;show_faces=false&amp;action=like&amp;colorscheme=light&amp;font&amp;height=21" scrolling="no" frameborder="0" style="border:none; overflow:hidden; width:50px; height:21px;" allowTransparency="true"></iframe>
     <!--
     <a href="https://twitter.com/share" class="twitter-share-button" data-url="@Url.Content("~/news/NewsDetails/")@item.Id" data-text="@item.Title)">Tweet</a>
    <script>    !function (d, s, id) { var js, fjs = d.getElementsByTagName(s)[0]; if (!d.getElementById(id)) { js = d.createElement(s); js.id = id; js.src = "//platform.twitter.com/widgets.js"; fjs.parentNode.insertBefore(js, fjs); } } (document, "script", "twitter-wjs");</script>
    -->
    </p>
    if(i <= 2){
        <hr style="height:0; border-width:2px 0 0 0;border-style:dotted;border-color:#cccccc;" />
    }
        i++;
    }
}
}